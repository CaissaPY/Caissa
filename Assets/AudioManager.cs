using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AudioManager : MonoBehaviour
{   
    
    // public GameObject backgroundMusicObject; // Referencia al objeto que contiene el AudioSource de fondo

    // private AudioSource backgroundMusic; // Referencia al AudioSource del objeto de fondo

    // Start is called before the first frame update
    void Start()
    {
        // backgroundMusic = backgroundMusicObject.GetComponent<AudioSource>();
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
